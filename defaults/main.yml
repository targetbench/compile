compile:
  compile_1:
    category: 'Performance application compile kernel-dev_01'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_16:
    category: 'Performance application compile kernel-dev_16'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_2:
    category: 'Performance application compile kernel-dev_02'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_32:
    category: 'Performance application compile kernel-dev_32'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_4:
    category: 'Performance application compile kernel-dev_04'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_64:
    category: 'Performance application compile kernel-dev_64'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_72:
    category: 'Performance application compile kernel-dev_72'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
  compile_8:
    category: 'Performance application compile kernel-dev_08'
    scores_way: 'exp_score_compute 5 -0.5'
    parser: 'compile_parser'
